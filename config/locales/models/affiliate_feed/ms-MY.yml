ms-MY:

  predefined:

    models:

      affiliate_feed:

  will_paginate:
    models:
      affiliate_feed:
        zero: "Entries"
        one: "Entry"
        few: "Entries"
        other: "Entries"
